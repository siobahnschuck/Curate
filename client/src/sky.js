import moment from 'moment'

export const startTime = () => {
  let day 
  let today = moment()
  let h = today.hour()

  switch (h === x) {
    case (x = 0):
      day = '#273D45'
      break;
    case (x = 1):
      day = "234654"
    default: 
      return 
  }
}